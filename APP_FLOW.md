# Harness Mobile 应用流程

状态：受控文档
路由源：[`AppRootView.swift`](HarnessMobile/App/AppRootView.swift)

## 启动状态机

```text
启动
  ├─ 本地状态未载入 → “正在载入本地会话”
  ├─ 未配置 → Setup / Onboarding
  └─ 已配置 → 首页 / 项目列表
```

App Intent、Share Extension 和 Widget 深链只在本地状态就绪且已配置后消费，然后进入对应会话。

## 首次配置

```text
选择服务商 → 填写连接与 API Key → 选择模型 → 阅读安全边界 → 保存 → 首页
```

首次配置只展示启动所需字段。高级推理、目录刷新和完整兼容说明在保存后的服务商编辑页提供。凭据保存到 Keychain，不进入会话或设置快照。

## 首页与项目

```text
首页
  ├─ 搜索/筛选项目
  ├─ 打开项目 → 对话
  ├─ 新建项目 → 对话
  ├─ 工具 → 工具总览
  └─ 设置 → 设置
```

当前没有独立 Project 持久化模型；会话标题作为项目名称。后台状态、诊断和权限不占首页主区域。

## 对话主流程

```text
输入文字/附件
  → 可选用户选择或计划审查
  → 模型流式响应
  → 工具调用审批
  → 本机 Swift 工具或 iSH 工具
  → 工具结果返回当前模型
  → 完成、取消、失败或可恢复中断
```

会话标题可打开“本会话模型”；选择其他模型自动创建会话覆盖。会话选项提供对话/轨迹、工具权限、导出和相关管理动作。

## 工具总览

| 入口 | 目的 |
| --- | --- |
| iSH 终端 | 在手机 Alpine 沙箱执行和查看命令 |
| 任务与轨迹 | 查看 Goal、Plan、Todo、Jobs 和 Harness 调用链 |
| 工作区 | 导入、浏览、编辑和导出本地文件 |
| Cordis 插件 | 管理原生与 Host-half 插件、市场和设置 |
| 设置 | 管理模型、后台、权限、Agent 与运行环境 |

## 权限流程

```text
用户或 Agent 触发能力
  → Harness 范围/风险审批
  → iOS 系统权限或可见控制器
  → 执行
  → 脱敏结果写入会话/轨迹并按需发送模型
```

Harness 永久授权不能跳过 iOS 的照片、联系人、定位、健康、认证、NFC 或文件选择流程。

## 插件流程

```text
市场/GitHub/ZIP
  → 来源与归档校验
  → 原生清单适配（优先）
  → iSH Host-half JavaScript（可兼容时）
  → 激活 generation
  → Tool/Prompt/设置/原生 Client 投影
  → 失败回滚或明确不可兼容
```

## 后台与恢复

```text
前台任务
  → 用户启用的系统后台能力
  → 有限后台窗口 / Continued Processing
  → 完成通知或 Live Activity
  → 系统到期时持久化 interrupted
  → 系统唤醒或回到前台后恢复
```

任何后台路径都不能承诺永久运行；真实接受标准见 [平台规范](PLATFORM_GUIDELINES.md)。

## 失败出口

- 配置/凭据错误：停在可编辑页面，不发送请求。
- 工具/权限拒绝：返回明确失败，不伪造工具结果。
- 插件失败：撤回本 generation 的贡献并保留旧版本或诊断。
- 系统中断：写入可恢复状态；没有恢复证据时不显示已完成。
- 导出失败：保留本地数据并显示系统错误。

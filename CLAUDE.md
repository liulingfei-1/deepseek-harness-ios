@AGENTS.md

## Claude Code bridge

- Work from the repository root. Before editing a scoped module, read its nearest `AGENTS.md`.
- Keep `AGENTS.md` as the canonical shared policy; do not copy its contents here.
- Keep private machine settings in ignored `CLAUDE.local.md`; never put API keys, device IDs, or test credentials in tracked instructions.
